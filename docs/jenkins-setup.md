# Jenkins CI/CD Setup

> Placeholder — Jenkins setup to be documented once implemented.

This will cover:
- Installing Jenkins on the VM (Docker container)
- Configuring the Jenkinsfile pipeline
- Setting up GitHub webhook to trigger Jenkins
- Storing credentials in Jenkins (GHCR token, SSH key, deploy token)
- How the Jenkins pipeline mirrors the GitHub Actions workflow

See [switching-ci.md](switching-ci.md) for how to toggle between GitHub Actions and Jenkins.
