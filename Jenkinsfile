// Jenkins pipeline — to be implemented
// See docs/jenkins-setup.md for setup instructions
pipeline {
    agent any
    stages {
        stage('Placeholder') {
            steps {
                echo 'Jenkins pipeline not yet configured'
            }
        }
    }
}
